# Choose Duck Page

Страница выбора утки для игры. Пользователь может выбрать утку из своей коллекции или купить новую.

## 🔧 Development Mode

Для тестирования с реальным API используйте dev токен:

1. Откройте `app/lib/devToken.ts`
2. Установите `USE_DEV_TOKEN = true` (уже включено по умолчанию)
3. Токен будет автоматически установлен при загрузке страницы

**В консоли браузера появится сообщение:** `🔧 Using DEV_TOKEN for testing`

### Ручное управление токеном

В консоли браузера доступны хелперы:

```javascript
// Импортируйте в компоненте и используйте
import { setDevToken, clearDevToken } from '../lib/devToken';

setDevToken();   // Установить токен
clearDevToken(); // Очистить токен
```

⚠️ **ВАЖНО:** Перед коммитом установите `USE_DEV_TOKEN = false` или удалите токен из `devToken.ts`!

## Структура

- **`/choose-duck/page.tsx`** - Главная страница с двумя вкладками
- **`/components/DuckCard.tsx`** - Компонент карточки утки
- **`/components/TabSwitch.tsx`** - Компонент переключателя вкладок

## Серая утка по умолчанию

⚠️ **ВАЖНО:** Каждый новый пользователь должен получать **серую утку (gray)** бесплатно при регистрации.

- Серая утка бесплатная
- Выдается автоматически
- Отображается во вкладке "Мои Утки" с сердечками ❤️

## API Integration

### Endpoint: `GET /user/ducks`

Возвращает данные о утках пользователя:

```typescript
interface DucksResponse {
  buyed: DuckFromAPI[];  // Купленные утки (вкладка "Мои Утки")
  ducks: DuckFromAPI[];  // Утки в магазине (вкладка "Купить")
}

interface DuckFromAPI {
  id: number;
  key: string;                // ключ утки для переводов ("Gray Duck", "Black Duck", etc.)
  image: string;              // URL изображения (может быть пустым, тогда используем локальные)
  lives: number;              // количество жизней (ТОЛЬКО для серой утки по умолчанию)
  opened: boolean;            // открыта ли утка (если false - кнопка "СКОРО")
  seconds_to_play: number;    // секунды до следующей игры (если <= 0 - можно играть)
  eggs_per_month: number;     // производство яиц в месяц
  eggs_per_one: number;       // сколько получаешь X за одно яйцо
  price: number;              // цена в USD (для магазина)
  pcs: number;                // количество в наличии (пока не используется)
  curent_from: {
    curent: number;           // сколько дней прошло
    from: number;             // всего дней
  };
}
```

### Endpoint: `GET /user/ducks/buy?id={id}`

Покупка утки по ID.

## Логика отображения

### Для "Мои Утки" (buyed):
1. **lives > 0** → показываем ❤️ с количеством жизней
2. **opened = false** → кнопка "СКОРО" (disabled, opacity не меняется)
3. **opened = true && seconds_to_play <= 0** → кнопка "ИГРАТЬ" (желтая, активная)
4. **opened = true && seconds_to_play > 0** → показываем таймер HH:MM:SS, кнопка "ИГРАТЬ" (серая, disabled)
5. **curent_from** → показываем "1/30 дн" в бейдже

### Для "Купить" (ducks):
1. **opened = false** → весь card с **opacity: 0.4**, кнопка "СКОРО" (disabled)
2. **opened = true** → кнопка "BUY $XX" (зеленая, активная)
3. Показываем **eggs_per_month** и **eggs_per_one** (1🥚 = X EGGS)
4. **curent_from** → показываем "1/30 дн" в бейдже

## Изображения уток

Локальные SVG находятся в `/public/main/ducks/`:
- `gray.svg` - Серая утка
- `dark.svg` - Черная утка
- `blue.svg` - Синяя утка
- `red.svg` - Красная утка
- `pink.svg` - Розовая утка
- `orange.svg` - Оранжевая утка
- `white.svg` - Белая утка
- `purple.svg` - Лиловая утка
- `wine.svg` - Винная утка
- `brownjn.svg` - Коричневая утка
- `gold.svg` - Золотая утка

Если API возвращает `image` (пустую строку или нет), используются локальные изображения.

## Компоненты

### DuckCard

Отображает карточку утки с:
- Изображением утки
- Бейджем статуса (жизни или дни)
- Названием утки
- Информацией (статус или eggs/month)
- Кнопкой действия (Играть / BUY / disabled)

**Props:**
```typescript
{
  duck: DuckFromAPI;
  onPlayClick?: (duckId: number) => void;
  onBuyClick?: (duckId: number) => void;
  isShopItem?: boolean;
}
```

### TabSwitch

Переключатель между вкладками "Мои Утки" и "Купить".

**Props:**
```typescript
{
  activeTab: DuckTab;
  onTabChange: (tab: DuckTab) => void;
  myDucksCount: number;
  shopDucksCount: number;
  myDucksLabel: string;
  shopLabel: string;
}
```

## Стили

Все стили реализованы с использованием **Tailwind CSS**:

- Градиенты для кнопок
- Тени и скругления
- Анимации и переходы
- Адаптивность

## Локализация

Поддерживаются переводы для:
- Названий уток (grayDuck, blackDuck, blueDuck2, etc.)
- Статусов (availableToPlay, lives)
- Кнопок (playButton, buyButton, soonButton)
- Заголовков (chooseDuck, eachDuckOneAttempt, myDucks, buyDucks)

## Навигация

- Открывается при нажатии на кнопку "PLAY" на главной странице
- Закрывается через Telegram Back Button (нативная кнопка Telegram)
- После покупки утки список обновляется автоматически

## Состояния

1. **Loading** - показывается спиннер во время загрузки
2. **Error** - показывается сообщение об ошибке с кнопкой Retry
3. **Empty** - если нет уток, показывается сообщение
4. **Loaded** - отображается список уток

## Взаимодействие

- **Haptic Feedback** при всех действиях пользователя
- **Active states** для кнопок и карточек
- **Cooldown timer** отображается в формате HH:MM:SS
- **Auto-refresh** после покупки утки

